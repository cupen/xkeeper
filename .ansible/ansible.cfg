# 本机部署默认配置：不配 inventory，依赖 ansible 隐式 localhost（免 inventory）。
# 下面两个开关只用于静默"隐式 localhost"的提示性 WARNING，不影响功能；
# 将来扩展远程多主机时，用 -i <inventory> 传入主机清单即可。
[defaults]
interpreter_python = auto_silent
localhost_warning = False

[inventory]
inventory_unparsed_warning = False
